% !TeX root = 系统动力学建模与仿真笔记.tex
\section{代数基础}

本节介绍多体系统动力学建模中所需要的矩阵知识。由于后续章节仅用到反对称矩阵与叉乘矩阵，这里只保留这一部分内容，其余矩阵分析内容从略。

\begin{mydefinition}{对称矩阵与反对称矩阵}
设 $\bm{A}$ 为 $n$ 阶方阵。若 $\bm{A}^{\mathrm{T}} = \bm{A}$，即 $a_{ij} = a_{ji}$，则称 $\bm{A}$ 为对称矩阵；若 $\bm{A}^{\mathrm{T}} = -\bm{A}$，即 $a_{ij} = -a_{ji}$，则称 $\bm{A}$ 为反对称矩阵。反对称矩阵的主对角元素均为零。
\end{mydefinition}

任一 $n$ 阶方阵 $\bm{A}$ 均可唯一分解为一个对称矩阵与一个反对称矩阵之和：
\begin{equation}\label{eq:矩阵:对称分解}
\bm{A} = \frac{\bm{A}+\bm{A}^{\mathrm{T}}}{2} + \frac{\bm{A}-\bm{A}^{\mathrm{T}}}{2}.
\end{equation}

反对称矩阵与三维向量的叉乘有着密切的联系。对任意向量 $\bm{a} = \begin{bmatrix} a_1 & a_2 & a_3 \end{bmatrix}^{\mathrm{T}}\in\mathbb{R}^3$，定义其叉乘矩阵为反对称矩阵
\begin{equation}\label{eq:矩阵:叉乘矩阵}
\bm{a}^{\times} \triangleq \begin{bmatrix} 0 & -a_3 & a_2 \\ a_3 & 0 & -a_1 \\ -a_2 & a_1 & 0 \end{bmatrix},
\end{equation}
则向量叉乘可以表示为矩阵乘法：
\begin{equation}\label{eq:矩阵:叉乘矩阵性质}
\bm{a}\times\bm{b} = \bm{a}^{\times}\,\bm{b}, \qquad (\bm{a}\times\bm{b})^{\times} = \bm{b}\bm{a}^{\mathrm{T}} - \bm{a}\bm{b}^{\mathrm{T}},
\end{equation}
式中 $\bm{a}\times\bm{b}$ 表示向量叉乘。叉乘矩阵在刚体动力学中具有重要作用，例如角速度向量与姿态变化率的关系即借助叉乘矩阵表示。为表述方便，常把 $\mathbf{q}\times$ 看做一个矩阵，这个矩阵记为 $\tilde{\mathbf{q}}$，其元素为
\begin{equation}
\tilde{\mathbf{q}}=\begin{bmatrix}
0 & -q_3 & q_2 \\
q_3 & 0 & -q_1 \\
-q_2 & q_1 & 0
\end{bmatrix},
\end{equation}
即 $\tilde{\mathbf{q}}$ 与式~\eqref{eq:矩阵:叉乘矩阵} 中的 $\bm{a}^{\times}$ 表示同一个对象，两种记法在不同文献中通用。

反对称矩阵还具有如下性质：对 $n$ 阶反对称矩阵 $\bm{A}$，
\begin{equation*}
\det(\bm{A}^{\mathrm{T}}) = \det(-\bm{A}) = (-1)^{n}\det(\bm{A}),
\end{equation*}
当 $n$ 为奇数时 $\det\bm{A} = 0$；且反对称矩阵的特征值位于虚轴上，特征向量相互正交（酉）。此外，任意方阵均可唯一分解为对称部分与反对称部分之和（式~\eqref{eq:矩阵:对称分解}），该分解在刚体动力学中对应应变率张量与旋率张量的分解。

\section{并矢}

并矢（dyadic）是描述二阶线性变换的数学工具，刚体动力学中的转动算子与转动惯量张量均可表示为并矢。本节介绍并矢的定义、运算及其与矩阵的联系。

\parahead{并矢的定义}

\begin{mydefinition}{并矢}
设 $\bm{a}$、$\bm{b}$ 为两个向量，则按确定顺序并列而成的量 $\bm{a}\bm{b}$ 称为并矢（dyad）。有限个并矢之和
\begin{equation}\label{eq:并矢:定义}
\bm{\Phi} = \sum_{i=1}^{N}\bm{a}_i\bm{b}_i
\end{equation}
称为并矢式（dyadic）。
\end{mydefinition}

并矢与向量的点积定义为
\begin{equation}\label{eq:并矢:点积}
\bm{a}\bm{b}\cdot\bm{v} = \bm{a}(\bm{b}\cdot\bm{v}), \qquad \bm{v}\cdot\bm{a}\bm{b} = (\bm{v}\cdot\bm{a})\bm{b},
\end{equation}
即并矢与向量的点积仍是向量；注意一般地 $\bm{\Phi}\cdot\bm{v}\neq\bm{v}\cdot\bm{\Phi}$，点积的顺序不可交换。

并矢式 $\bm{\Phi}$ 关于 $\bm{v}$ 的映射 $\bm{v}\mapsto\bm{\Phi}\cdot\bm{v}$ 是线性的，故并矢式是二阶线性变换的表示。反之，任意将向量映射为向量的线性变换均可表示为并矢式，且至多需要三个并矢：

\begin{myTheorem}{并矢的基展开}
设 $\mathbf{e}_1,\mathbf{e}_2,\mathbf{e}_3$ 为一组右手正交单位向量，则任意并矢式 $\bm{\Phi}$ 可表示为
\begin{equation}\label{eq:并矢:展开}
\bm{\Phi} = \bm{\Phi}\cdot\mathbf{e}_1\,\mathbf{e}_1 + \bm{\Phi}\cdot\mathbf{e}_2\,\mathbf{e}_2 + \bm{\Phi}\cdot\mathbf{e}_3\,\mathbf{e}_3.
\end{equation}
\end{myTheorem}

\begin{proof}
对任意向量 $\bm{v}$，由单位向量组的完备性 $\displaystyle\sum_{i=1}^{3}\mathbf{e}_i(\mathbf{e}_i\cdot\bm{v}) = \bm{v}$ 可得
$$
\left(\sum_{i=1}^{3}\bm{\Phi}\cdot\mathbf{e}_i\,\mathbf{e}_i\right)\cdot\bm{v}
= \sum_{i=1}^{3}(\bm{\Phi}\cdot\mathbf{e}_i)(\mathbf{e}_i\cdot\bm{v})
= \bm{\Phi}\cdot\sum_{i=1}^{3}\mathbf{e}_i(\mathbf{e}_i\cdot\bm{v})
= \bm{\Phi}\cdot\bm{v}.
$$
由 $\bm{v}$ 的任意性即知式~\eqref{eq:并矢:展开} 成立。
\end{proof}

\parahead{并矢的运算}

并矢式之间可进行加法与数乘运算，按并矢的线性性质逐项进行，并满足结合律与分配律。两个并矢式的点积定义为
\begin{equation}\label{eq:并矢:并矢点积}
\bm{a}\bm{b}\cdot\bm{c}\bm{d} = (\bm{b}\cdot\bm{c})\,\bm{a}\bm{d},
\end{equation}
即先对相邻向量作点积，结果为并矢式；一般地 $\bm{\Phi}\cdot\bm{\Psi}\neq\bm{\Psi}\cdot\bm{\Phi}$。

并矢式与向量的叉积定义为
\begin{equation}\label{eq:并矢:叉积}
\bm{a}\bm{b}\times\bm{v} = \bm{a}(\bm{b}\times\bm{v}), \qquad \bm{v}\times\bm{a}\bm{b} = (\bm{v}\times\bm{a})\bm{b},
\end{equation}
并矢式与向量的叉积仍是并矢式。旋转并矢（见本节末尾）正是借助并矢式与向量的叉积构造的。

\parahead{单位并矢}

\begin{mydefinition}{单位并矢}
设 $\mathbf{e}_1,\mathbf{e}_2,\mathbf{e}_3$ 为一组右手正交单位向量，则
\begin{equation}\label{eq:并矢:单位并矢}
\bm{U} = \mathbf{e}_1\mathbf{e}_1 + \mathbf{e}_2\mathbf{e}_2 + \mathbf{e}_3\mathbf{e}_3
\end{equation}
称为单位并矢。
\end{mydefinition}

单位并矢具有如下性质：
\begin{enumerate}
\item 对任意向量 $\bm{v}$，$\bm{U}\cdot\bm{v} = \bm{v}\cdot\bm{U} = \bm{v}$；
\item 对任意并矢式 $\bm{\Phi}$，$\bm{U}\cdot\bm{\Phi} = \bm{\Phi}\cdot\bm{U} = \bm{\Phi}$；
\item $\bm{U}^{\mathrm{T}} = \bm{U}$，即单位并矢为对称并矢；
\item 对任意向量 $\bm{a}$、$\bm{b}$，$\bm{a}\cdot\bm{U}\cdot\bm{b} = \bm{a}\cdot\bm{b}$。
\end{enumerate}

\parahead{并矢的转置}

\begin{mydefinition}{并矢的转置}
并矢 $\bm{a}\bm{b}$ 的转置定义为
\begin{equation}\label{eq:并矢:转置定义}
(\bm{a}\bm{b})^{\mathrm{T}} = \bm{b}\bm{a},
\end{equation}
并矢式 $\bm{\Phi} = \sum_{i}\bm{a}_i\bm{b}_i$ 的转置定义为
\begin{equation}\label{eq:并矢:转置}
\bm{\Phi}^{\mathrm{T}} = \sum_{i}\bm{b}_i\bm{a}_i.
\end{equation}
\end{mydefinition}

转置运算满足 $(\bm{\Phi}^{\mathrm{T}})^{\mathrm{T}} = \bm{\Phi}$、$(\bm{\Phi}+\bm{\Psi})^{\mathrm{T}} = \bm{\Phi}^{\mathrm{T}}+\bm{\Psi}^{\mathrm{T}}$ 以及 $(\bm{\Phi}\cdot\bm{\Psi})^{\mathrm{T}} = \bm{\Psi}^{\mathrm{T}}\cdot\bm{\Phi}^{\mathrm{T}}$。若 $\bm{\Phi}^{\mathrm{T}} = \bm{\Phi}$，则称 $\bm{\Phi}$ 为对称并矢；若 $\bm{\Phi}^{\mathrm{T}} = -\bm{\Phi}$，则称 $\bm{\Phi}$ 为反对称并矢。任一并矢式均可唯一分解为对称部分与反对称部分之和：
\begin{equation}\label{eq:并矢:对称分解}
\bm{\Phi} = \frac{\bm{\Phi}+\bm{\Phi}^{\mathrm{T}}}{2} + \frac{\bm{\Phi}-\bm{\Phi}^{\mathrm{T}}}{2}.
\end{equation}

\parahead{并矢的矩阵表示}

设 $\mathbf{e}_1,\mathbf{e}_2,\mathbf{e}_3$ 为一组右手正交单位向量，则并矢式 $\bm{\Phi}$ 可展开为
\begin{equation}\label{eq:并矢:分量展开}
\bm{\Phi} = \sum_{i=1}^{3}\sum_{j=1}^{3}\Phi_{ij}\,\mathbf{e}_i\mathbf{e}_j,
\end{equation}
式中分量 $\Phi_{ij} = \mathbf{e}_i\cdot\bm{\Phi}\cdot\mathbf{e}_j$。于是 $\bm{\Phi}$ 与该基下的矩阵
\begin{equation}\label{eq:并矢:矩阵表示}
[\bm{\Phi}] = \begin{bmatrix} \Phi_{11} & \Phi_{12} & \Phi_{13} \\ \Phi_{21} & \Phi_{22} & \Phi_{23} \\ \Phi_{31} & \Phi_{32} & \Phi_{33} \end{bmatrix}
\end{equation}
一一对应，并矢与向量的点积对应矩阵与向量的乘法：
\begin{equation}\label{eq:并矢:矩阵点积}
[\bm{\Phi}\cdot\bm{v}] = [\bm{\Phi}][\bm{v}], \qquad [\bm{v}\cdot\bm{\Phi}] = [\bm{v}]^{\mathrm{T}}[\bm{\Phi}],
\end{equation}
并矢的转置对应矩阵的转置，即 $[\bm{\Phi}^{\mathrm{T}}] = [\bm{\Phi}]^{\mathrm{T}}$。

若 $\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3$ 与 $\mathbf{b}_1,\mathbf{b}_2,\mathbf{b}_3$ 分别为两个坐标系中的右手正交单位向量，则描述两坐标系相对姿态的并矢 $\bm{\sigma}$ 可表示为
\begin{equation}\label{eq:并矢:方向余弦并矢}
\bm{\sigma} = \sum_{i=1}^{3}\sum_{j=1}^{3} a_{ij}\,\mathbf{a}_i\mathbf{b}_j, \qquad a_{ij} = \mathbf{a}_i\cdot\bm{\sigma}\cdot\mathbf{b}_j,
\end{equation}
式中 $a_{ij}$ 即为方向余弦矩阵的元素（见第~\ref{chap:kinematics}~章第~\ref{sec:indirect-orientation}~节）。这体现了并矢是坐标无关的几何量，而矩阵只是并矢在某组基下的具体表示。

\parahead{旋转并矢}

在简单转动（见第~\ref{chap:kinematics}~章）中，向量 $\bm{a}$ 绕单位向量 $\bm{\lambda}$ 旋转角 $\theta$ 得到向量 $\bm{b}$，二者满足 Rodrigues 公式
\begin{equation}\label{eq:并矢:旋转公式}
\bm{b} = \bm{a}\cos\theta - (\bm{a}\times\bm{\lambda})\sin\theta + \bm{\lambda}(\bm{\lambda}\cdot\bm{a})(1-\cos\theta).
\end{equation}
上式右端是 $\bm{a}$ 的线性函数，故可表示为某个并矢与 $\bm{a}$ 的点积：

\begin{myTheorem}{旋转并矢}
\begin{equation}\label{eq:并矢:旋转并矢}
\bm{A} = \bm{U}\cos\theta - (\bm{U}\times\bm{\lambda})\sin\theta + \bm{\lambda}\bm{\lambda}(1-\cos\theta)
\end{equation}
称为绕 $\bm{\lambda}$ 轴旋转角 $\theta$ 的旋转并矢，满足 $\bm{b} = \bm{a}\cdot\bm{A}$。
\end{myTheorem}

\begin{proof}
对任意向量 $\bm{a}$，
$$
\bm{a}\cdot\bm{A}
= \bm{a}\cdot\bm{U}\cos\theta - \bm{a}\cdot(\bm{U}\times\bm{\lambda})\sin\theta + \bm{a}\cdot\bm{\lambda}\bm{\lambda}(1-\cos\theta).
$$
由单位并矢性质 $\bm{a}\cdot\bm{U} = \bm{a}$，并矢叉积定义 $\bm{a}\cdot(\bm{U}\times\bm{\lambda}) = \bm{a}\times\bm{\lambda}$ 以及 $\bm{a}\cdot\bm{\lambda}\bm{\lambda} = \bm{\lambda}(\bm{\lambda}\cdot\bm{a})$，即得
$$
\bm{a}\cdot\bm{A} = \bm{a}\cos\theta - (\bm{a}\times\bm{\lambda})\sin\theta + \bm{\lambda}(\bm{\lambda}\cdot\bm{a})(1-\cos\theta) = \bm{b},
$$
与式~\eqref{eq:并矢:旋转公式} 一致。
\end{proof}

由并矢的矩阵表示可知，旋转并矢 $\bm{A}$ 在某组基下的矩阵即该旋转的方向余弦矩阵，二者是同一几何对象在不同表示体系下的体现。此外，刚体动力学中的转动惯量张量即为对称并矢，其矩阵表示就是惯量矩阵，这将在后续章节中详细讨论。

\section{笛卡尔张量}

并矢一节从几何对象的角度讨论了二阶线性变换，本节转入它的坐标基础：引入右手笛卡尔直角坐标系的单位矢量基，建立矢量及其点积、叉积、混合积等运算的坐标（矩阵）表示。这套形式记号是后续章节中方向余弦矩阵、角速度列阵与动能计算的公共工具。

\parahead{坐标系、坐标轴上的单位矢量与矢量基}

\parahead{右手笛卡尔直角坐标系及其坐标轴上的单位矢量}

在右手笛卡尔直角坐标系中，沿三根坐标轴取单位矢量 $\mathbf{e}_1$、$\mathbf{e}_2$、$\mathbf{e}_3$，构成右手正交单位向量组。由正交归一性，任意两个单位矢量的点积为
\begin{equation}\label{eq:笛卡尔张量:单位矢量点积}
\mathbf{e}_m \cdot \mathbf{e}_n = \delta_{mn}, \qquad
\delta_{mn} =
\begin{cases}
1, & m = n, \\
0, & m \neq n,
\end{cases}
\end{equation}
式中 $\delta_{mn}$ 称为 Kronecker 符号。叉积则为
\begin{equation}\label{eq:笛卡尔张量:单位矢量叉积}
\mathbf{e}_m \times \mathbf{e}_n = \sum_{l=1}^{3} \varepsilon_{mnl}\,\mathbf{e}_l, \qquad
\varepsilon_{mnl} =
\begin{cases}
1, & (m,n,l)=(1,2,3)\ \text{或}\ (2,3,1)\ \text{或}\ (3,1,2), \\
-1, & (m,n,l)=(2,1,3)\ \text{或}\ (3,2,1)\ \text{或}\ (1,3,2), \\
0, & m=n\ \text{或}\ n=l\ \text{或}\ l=m,
\end{cases}
\end{equation}
$\varepsilon_{mnl}$ 称为置换符号。

\parahead{右手笛卡尔直角坐标系矢量基及其运算法则}

将三个单位矢量按列、按行排成阵列，
\begin{equation}\label{eq:笛卡尔张量:矢量基}
\bm{e} \triangleq
\begin{bmatrix} \mathbf{e}_1 \\ \mathbf{e}_2 \\ \mathbf{e}_3 \end{bmatrix}, \qquad
\bm{e}^{\mathrm{T}} \triangleq
\begin{bmatrix} \mathbf{e}_1 & \mathbf{e}_2 & \mathbf{e}_3 \end{bmatrix},
\end{equation}
称为右手笛卡尔直角坐标系的矢量基。矢量基仅是形式记号：它把三个矢量排成阵列，其元素是矢量而非数量。后续章节将以带下标的形式引用它，如 $\bm{e}_O$、$\bm{e}_R$ 分别表示 $O$ 系与 $R$ 系的矢量基。

规定矢量基的乘法按对应元素的点积与叉积逐项进行，直接展开可得
\begin{gather}
\bm{e}^{\mathrm{T}} \cdot \bm{e}
= \mathbf{e}_1\cdot\mathbf{e}_1 + \mathbf{e}_2\cdot\mathbf{e}_2 + \mathbf{e}_3\cdot\mathbf{e}_3 = 3,
\label{eq:笛卡尔张量:eTe}\\[2pt]
\bm{e} \cdot \bm{e}^{\mathrm{T}}
=
\begin{bmatrix}
\mathbf{e}_1\!\cdot\!\mathbf{e}_1 & \mathbf{e}_1\!\cdot\!\mathbf{e}_2 & \mathbf{e}_1\!\cdot\!\mathbf{e}_3 \\
\mathbf{e}_2\!\cdot\!\mathbf{e}_1 & \mathbf{e}_2\!\cdot\!\mathbf{e}_2 & \mathbf{e}_2\!\cdot\!\mathbf{e}_3 \\
\mathbf{e}_3\!\cdot\!\mathbf{e}_1 & \mathbf{e}_3\!\cdot\!\mathbf{e}_2 & \mathbf{e}_3\!\cdot\!\mathbf{e}_3
\end{bmatrix}
= \bm{I}_3,
\label{eq:笛卡尔张量:eeT}\\[2pt]
\bm{e}^{\mathrm{T}} \times \bm{e}
= \mathbf{e}_1\times\mathbf{e}_1 + \mathbf{e}_2\times\mathbf{e}_2 + \mathbf{e}_3\times\mathbf{e}_3 = \bm{0},
\label{eq:笛卡尔张量:eTxe}\\[2pt]
\bm{e} \times \bm{e}^{\mathrm{T}}
=
\begin{bmatrix}
\bm{0} & \mathbf{e}_3 & -\mathbf{e}_2 \\
-\mathbf{e}_3 & \bm{0} & \mathbf{e}_1 \\
\mathbf{e}_2 & -\mathbf{e}_1 & \bm{0}
\end{bmatrix}
= -\tilde{\bm{e}}.
\label{eq:笛卡尔张量:exeT}
\end{gather}
式~\eqref{eq:笛卡尔张量:exeT} 中的 $\tilde{\bm{e}}$ 是叉乘矩阵的形式推广。对任意三维列阵 $\bm{u}=\begin{bmatrix}u_1&u_2&u_3\end{bmatrix}^{\mathrm{T}}$，定义其叉乘矩阵为反对称矩阵
\begin{equation}\label{eq:笛卡尔张量:叉乘矩阵定义}
\tilde{\bm{u}} \triangleq
\begin{bmatrix}
0 & -u_3 & u_2 \\
u_3 & 0 & -u_1 \\
-u_2 & u_1 & 0
\end{bmatrix},
\qquad \tilde{u}(m,n) = -\sum_{l=1}^{3}\varepsilon_{mnl}\,u_l,
\end{equation}
它与式~\eqref{eq:矩阵:叉乘矩阵} 定义的叉乘矩阵 $\bm{u}^{\times}$ 是同一对象，两种记法通用。叉乘矩阵具有如下性质：
\begin{equation}\label{eq:笛卡尔张量:叉乘矩阵性质}
\tilde{\bm{u}}^{\mathrm{T}} = -\tilde{\bm{u}}, \qquad
\tilde{\bm{u}}\,\bm{v} = -\tilde{\bm{v}}\,\bm{u}, \qquad
\bm{v}^{\mathrm{T}}\tilde{\bm{u}} = -\bm{u}^{\mathrm{T}}\tilde{\bm{v}}, \qquad
\tilde{\bm{u}}\,\bm{u} = \bm{0},
\end{equation}
\begin{equation}\label{eq:笛卡尔张量:叉乘矩阵双乘}
\tilde{\bm{u}}\,\tilde{\bm{v}} = \bm{v}\bm{u}^{\mathrm{T}} - \left(\bm{u}^{\mathrm{T}}\bm{v}\right)\bm{I}_3.
\end{equation}

\parahead{矢量及矢量运算的坐标表示}

在不引起混淆的前提下，本小节用同一粗体字母表示矢量与其坐标列阵，由上下文区分。

\parahead{矢量的坐标表示}

任意矢量沿基矢量的分解可写成矩阵乘积（数乘）的形式：
\begin{equation}\label{eq:笛卡尔张量:矢量分解}
\bm{u} = u_1\mathbf{e}_1 + u_2\mathbf{e}_2 + u_3\mathbf{e}_3
= \bm{e}^{\mathrm{T}}\bm{u} = \bm{u}^{\mathrm{T}}\bm{e},
\end{equation}
式中 $\bm{u} = \begin{bmatrix} u_1 & u_2 & u_3 \end{bmatrix}^{\mathrm{T}}$ 称为矢量 $\bm{u}$ 的坐标列阵（投影列阵）。

\parahead{两矢量点积运算的坐标表示}

利用式~\eqref{eq:笛卡尔张量:eeT}，两矢量的点积（内积）为
\begin{equation}\label{eq:笛卡尔张量:点积}
\bm{u}\cdot\bm{v}
= \left(\bm{u}^{\mathrm{T}}\bm{e}\right)\cdot\left(\bm{e}^{\mathrm{T}}\bm{v}\right)
= \bm{u}^{\mathrm{T}}\bm{v}.
\end{equation}
点积具有如下性质：
\begin{enumerate}
\item $\bm{u}\cdot\bm{u} = \bm{u}^{\mathrm{T}}\bm{u} = |\bm{u}|^2$，即矢量模长的平方；
\item $\bm{u}\cdot\bm{v} = \bm{u}^{\mathrm{T}}\bm{v} = \bm{v}^{\mathrm{T}}\bm{u} = \bm{v}\cdot\bm{u}$，点积满足交换律；
\item 比较 $|\bm{u}-\bm{v}|^2 = |\bm{u}|^2 + |\bm{v}|^2 - 2\,\bm{u}\cdot\bm{v}$ 与余弦定理 $|\bm{u}-\bm{v}|^2 = |\bm{u}|^2 + |\bm{v}|^2 - 2|\bm{u}||\bm{v}|\cos\langle\bm{u},\bm{v}\rangle$，可得
\begin{equation}\label{eq:笛卡尔张量:点积夹角}
\bm{u}\cdot\bm{v} = |\bm{u}|\,|\bm{v}|\cos\langle\bm{u},\bm{v}\rangle.
\end{equation}
\end{enumerate}

\parahead{两矢量叉积运算的坐标表示}

将叉积按基矢量展开并代入式~\eqref{eq:笛卡尔张量:单位矢量叉积}，得
\begin{equation}\label{eq:笛卡尔张量:叉积}
\bm{u}\times\bm{v}
= \sum_{i=1}^{3}\sum_{j=1}^{3} u_i v_j \left(\mathbf{e}_i\times\mathbf{e}_j\right)
= \bm{e}^{\mathrm{T}}\tilde{\bm{u}}\,\bm{v},
\end{equation}
即叉积等于矢量基行阵左乘叉乘矩阵再乘坐标列阵。叉积具有如下性质：
\begin{enumerate}
\item 反交换性：$\bm{u}\times\bm{v} = \bm{e}^{\mathrm{T}}\tilde{\bm{u}}\,\bm{v} = -\bm{e}^{\mathrm{T}}\tilde{\bm{v}}\,\bm{u} = -\bm{v}\times\bm{u}$；
\item $\bm{u}\cdot\left(\bm{u}\times\bm{v}\right) = \bm{u}^{\mathrm{T}}\tilde{\bm{u}}\,\bm{v} = 0$，同理 $\bm{v}\cdot\left(\bm{u}\times\bm{v}\right) = -\bm{v}^{\mathrm{T}}\tilde{\bm{v}}\,\bm{u} = 0$，即叉积同时垂直于参与运算的两个矢量；
\item 由 $|\bm{u}\times\bm{v}|^2 = \bm{v}^{\mathrm{T}}\tilde{\bm{u}}^{\mathrm{T}}\tilde{\bm{u}}\,\bm{v} = |\bm{u}|^2|\bm{v}|^2 - (\bm{u}\cdot\bm{v})^2$ 及式~\eqref{eq:笛卡尔张量:点积夹角} 可得
\begin{equation}\label{eq:笛卡尔张量:叉积模}
|\bm{u}\times\bm{v}| = |\bm{u}|\,|\bm{v}|\sin\langle\bm{u},\bm{v}\rangle.
\end{equation}
\end{enumerate}

\parahead{三矢量混合积运算的坐标表示}

三矢量的混合积为
\begin{equation}\label{eq:笛卡尔张量:混合积}
\bm{u}\cdot\left(\bm{v}\times\bm{w}\right)
= \left(\bm{u}^{\mathrm{T}}\bm{e}\right)\cdot\left(\bm{e}^{\mathrm{T}}\tilde{\bm{v}}\,\bm{w}\right)
= \bm{u}^{\mathrm{T}}\tilde{\bm{v}}\,\bm{w},
\end{equation}
由式~\eqref{eq:笛卡尔张量:叉乘矩阵性质} 可知混合积具有轮换不变性：
\begin{equation}\label{eq:笛卡尔张量:混合积轮换}
\bm{u}\cdot\left(\bm{v}\times\bm{w}\right)
= \bm{v}^{\mathrm{T}}\tilde{\bm{w}}\,\bm{u} = \bm{v}\cdot\left(\bm{w}\times\bm{u}\right)
= \bm{w}^{\mathrm{T}}\tilde{\bm{u}}\,\bm{v} = \bm{w}\cdot\left(\bm{u}\times\bm{v}\right).
\end{equation}

\parahead{三矢量叉积运算的坐标表示}

三矢量的叉积可写成两个叉乘矩阵的复合：
\begin{equation}\label{eq:笛卡尔张量:三重叉积}
\bm{u}\times\left(\bm{v}\times\bm{w}\right)
= \left(\bm{u}^{\mathrm{T}}\bm{e}\right)\times\left(\bm{e}^{\mathrm{T}}\tilde{\bm{v}}\,\bm{w}\right)
= \bm{e}^{\mathrm{T}}\tilde{\bm{u}}\,\tilde{\bm{v}}\,\bm{w}.
\end{equation}
代入式~\eqref{eq:笛卡尔张量:叉乘矩阵双乘} 展开，得常用的三重叉积展开式
\begin{equation}\label{eq:笛卡尔张量:三重叉积展开}
\bm{u}\times\left(\bm{v}\times\bm{w}\right)
= \bm{e}^{\mathrm{T}}\left[\bm{v}\left(\bm{u}^{\mathrm{T}}\bm{w}\right) - \left(\bm{u}^{\mathrm{T}}\bm{v}\right)\bm{w}\right]
= \left(\bm{u}\cdot\bm{w}\right)\bm{v} - \left(\bm{u}\cdot\bm{v}\right)\bm{w}.
\end{equation}

至此，矢量及其基本运算均已化为矩阵运算。结合并矢的矩阵表示（式~\eqref{eq:并矢:矩阵表示}），几何量（矢量、并矢）与坐标量（列阵、矩阵）的两条线索在本章内互相衔接，后续章节中方向余弦矩阵及其时间变化率、角速度列阵的定义以及动能的二次型表达均以此为基础。

\section{系统动力学方程数值求解}

本节介绍常微分方程与微分代数方程初值问题的基本数值求解方法，是后续章节中系统运动微分方程（第~\ref{chap:eom}~章）数值仿真计算的基础；文中涉及的质量矩阵 $\bm{M}$、约束雅可比 $\bm{\Phi}$ 等记号亦见该章。


\parahead{常微分方程数值求解的计算精度与稳定性}

一阶线性齐次常系数常微分方程的初值问题可表示为
\begin{equation*}
    \begin{cases}
        \dot{y} = -ay, & a > 0,\\[4pt]
        y(t)\big|_{t=0} = y_0, & y_0 > 0.
    \end{cases}
\end{equation*}
该微分方程初值问题存在解析解
\begin{equation*}
    y(t) = y_0\,\mathrm{e}^{-at},
\end{equation*}
如下图所示。
\begin{figure}[H]
    \centering
    \IfFileExists{Figure/指数衰减解析解.png}{
        \includegraphics[width=0.6\textwidth]{Figure/指数衰减解析解.png}
    }{
        \fbox{\parbox{0.5\textwidth}{\centering\vspace{3cm}%
        \textbf{[图片待插入]}\\[4pt] $y(t) = y_0\,e^{-at}$ 指数衰减曲线\\[4pt]\vspace{3cm}}}
    }
    \caption{$y(t) = y_0\,\mathrm{e}^{-at}$ 指数衰减解析解}
    \label{fig:exp_decay_exact}
\end{figure}

在进行数值求解时需将时间 $t$ 离散，因为解微分方程时计算机只能一个点一个点地算，所以把连续时间 $t$ 切成等长小段，段长 $\Delta t$：$t_k = k\Delta t$，离散后的时间 $t$ 和状态变量 $y$ 可记为 $t_k = k\Delta t$，$\hat{y}_k = \hat{y}(t_k)$，($k = 0,1,2,\cdots$)。：第 $k$ 个时刻就是"第 $k$ 段"，$k=0,1,2,\dots$，$\hat{y}_k = \hat{y}(t_k)$：*$\hat{y}$（带帽）表示数值近似解*，$y$（不带帽）是精确解。下标 $k$ 是第 $k$ 个时刻的值。状态变量 $y$ 的泰勒展开式为
\begin{equation*}
    \hat{y}_{k+1} = \hat{y}_k + \frac{\mathrm{d}\hat{y}_k}{\mathrm{d}t}\Delta t + \frac{1}{2!}\frac{\mathrm{d}^2\hat{y}_k}{\mathrm{d}t^2}\Delta t^2 + \frac{1}{3!}\frac{\mathrm{d}^3\hat{y}_k}{\mathrm{d}t^3}\Delta t^3 + o(\Delta t^3).
\end{equation*}
状态变量 $y$ 的一阶泰勒展开式（一阶精度）为
\begin{equation}
    \hat{y}_{k+1} = \hat{y}_k + \left[\alpha\,\frac{\mathrm{d}\hat{y}_k}{\mathrm{d}t} + (1-\alpha)\frac{\mathrm{d}\hat{y}_{k+1}}{\mathrm{d}t}\right]\Delta t + o(\Delta t),\quad \alpha \in [0,1].
    \label{eq:alpha_scheme}
\end{equation}

\parahead{向前欧拉法（显式积分）}

如果令 $\alpha = 1$（向前欧拉，显式积分），则有
\begin{align}
    \hat{y}_{k+1} &= \hat{y}_k + \frac{\mathrm{d}\hat{y}_k}{\mathrm{d}t}\Delta t \notag\\
    &= \hat{y}_k - a\hat{y}_k\Delta t \notag\\
    &= (1-a\Delta t)\hat{y}_k.
\end{align}
向前欧拉显式积分格式
\begin{equation}
    \hat{y}_{k+1} = \hat{y}_k - a\hat{y}_k\Delta t
    \;\Longrightarrow\;
    \hat{y}_{k+1} = (1-a\Delta t)\hat{y}_k
    \label{eq:forward_euler}
\end{equation}
由式~\eqref{eq:forward_euler}可知，每递推一步相当于将当前值乘以放大因子$(1-a\Delta t)$，即
\begin{equation}
    \hat{y}_k = (1-a\Delta t)^k y_0.
\end{equation}
解析解$y(t) = y_0\,\mathrm{e}^{-at}$随时间单调衰减并趋于零，为使数值解同样保持衰减不发散，须要求放大因子的模小于$1$，即
\begin{equation}
    |1-a\Delta t| < 1
    \quad\Longleftrightarrow\quad
    0 < a\Delta t < 2
    \quad\Longleftrightarrow\quad
    \Delta t < \frac{2}{a}.
\end{equation}
当$1 < a\Delta t < 2$时，数值解虽仍衰减，但每步变号，呈锯齿状振荡；当$a\Delta t > 2$时，$|1-a\Delta t| > 1$，数值解振荡放大直至发散。可见向前欧拉法的稳定性受步长限制：仅当$\Delta t < 2/a$时数值计算稳定，即\textbf{条件稳定}，如下图所示。
\begin{figure}[H]
    \centering
    \IfFileExists{Figure/向前欧拉法.png}{
        \includegraphics[width=0.7\textwidth]{Figure/向前欧拉法.png}
    }{
        \fbox{\parbox{0.6\textwidth}{\centering\vspace{3cm}%
        \textbf{[图片待插入]}\\[4pt] 向前欧拉法：不同步长数值解对比\\[4pt]\vspace{3cm}}}
    }
    \caption{向前欧拉法：不同步长 $\Delta t$ 下的数值解对比（条件稳定）}
    \label{fig:forward_euler_stability}
\end{figure}

\parahead{向后欧拉法（隐式积分）}

如果令 $\alpha = 0$（向后欧拉，隐式积分），则有
\begin{equation}
    \hat{y}_{k+1} = \hat{y}_k + \frac{\mathrm{d}\hat{y}_{k+1}}{\mathrm{d}t}\Delta t = \hat{y}_k - a\hat{y}_{k+1}\Delta t,
    \label{eq:backward_euler_raw}
\end{equation}
对于该线性微分方程，上式又可整理为
\begin{equation*}
    \hat{y}_{k+1} = \hat{y}_k / (1+a\Delta t).
\end{equation*}
向后欧拉隐式积分格式
\begin{equation}
    \hat{y}_{k+1} = \hat{y}_k - a\hat{y}_{k+1}\Delta t
    \;\Longrightarrow\;
    \hat{y}_{k+1} = \hat{y}_k / (1+a\Delta t)
    \label{eq:backward_euler}
\end{equation}
其每一步的放大因子为$1/(1+a\Delta t)$。由于$a > 0$、$\Delta t > 0$时恒有
\begin{equation}
    \left|\frac{1}{1+a\Delta t}\right| = \frac{1}{1+a\Delta t} < 1,
\end{equation}
数值解对任意步长均单调衰减、不会发散，稳定性不受步长限制，即\textbf{绝对稳定}（无条件稳定），如下图所示。绝对稳定的代价是积分格式为隐式：每个时间步均需关于$\hat{y}_{k+1}$求解方程，对于非线性问题需迭代求解。
\begin{figure}[H]
    \centering
    \IfFileExists{Figure/向后欧拉法.png}{
        \includegraphics[width=0.7\textwidth]{Figure/向后欧拉法.png}
    }{
        \fbox{\parbox{0.6\textwidth}{\centering\vspace{3cm}%
        \textbf{[图片待插入]}\\[4pt] 向后欧拉法：不同步长数值解对比\\[4pt]\vspace{3cm}}}
    }
    \caption{向后欧拉法：不同步长 $\Delta t$ 下的数值解对比（绝对稳定）}
    \label{fig:backward_euler_stability}
\end{figure}

\begin{remark}
上述稳定性差异在实际仿真中意义重大。多体系统往往同时包含慢变的整体运动与快变的局部高频成分（如刚性较大的约束、接触碰撞等引入的高频模态）。显式格式的稳定步长受系统中最快分量的时间尺度限制（本例中为$2/a$），即使高频成分并非所关心的内容，一旦步长选取过大，数值解便会发散；隐式格式则不受此限制，允许以远大于显式稳定步长的$\Delta t$稳定推进。因此，对于同时含有快、慢时间尺度的刚性问题（stiff problem），通常采用隐式方法求解；显式方法则多用于非刚性问题或对计算效率要求较高的场合。
\end{remark}


\parahead{将高阶常微分方程转化为一阶常微分方程组}

重力作用下单摆的运动微分方程可表示为
\begin{equation*}
    ml^2\ddot{\theta} = -mgl\sin\theta,
\end{equation*}
这是一个二阶非线性常微分方程，系统初始条件可表示为
\begin{equation*}
    \begin{cases}
        \theta(t)\big|_{t=t_0} = \theta_0,\\[4pt]
        \dot{\theta}(t)\big|_{t=t_0} = \dot{\theta}_0.
    \end{cases}
\end{equation*}
为便于对该二阶非线性常微分方程的初值问题进行数值求解，可以将其转化为一阶常微分方程组的初值问题。重力作用下单摆的运动微分方程
\begin{equation*}
    ml^2\ddot{\theta} = -mgl\sin\theta
\end{equation*}
可转化为
\begin{equation*}
    \begin{bmatrix}
        \dot{y}_1\\[3pt] \dot{y}_2
    \end{bmatrix}
    =
    \begin{bmatrix}
        y_2\\[3pt] -g\sin y_1/l
    \end{bmatrix},
    \qquad
    \begin{cases}
        y_1 \triangleq \theta,\\[3pt]
        y_2 \triangleq \dot{\theta}.
    \end{cases}
\end{equation*}
给定系统初始状态（包括初始位置和初始速度）
\begin{equation*}
    \begin{cases}
        y_1(t)\big|_{t=0} = y_1(0),\\[4pt]
        y_2(t)\big|_{t=0} = y_2(0),
    \end{cases}
\end{equation*}
存在众多成熟的微分方程数值求解方法来进行数值计算。


\parahead{一阶常微分方程组初值问题的一般形式}

一阶常微分方程组的初值问题的一般形式可记为
\begin{equation}
    \begin{cases}
        \bm{y}' = \bm{f}(t,\bm{y}), & t \in [a,b],\\[4pt]
        \bm{y}(a) = \bm{y}_0.
    \end{cases}
    \label{eq:ode_ivp}
\end{equation}
式中
\begin{equation*}
    \bm{y}(t) = \begin{bmatrix} y_1(t)\\ y_2(t)\\ \vdots\\ y_m(t) \end{bmatrix} \in \mathbb{R}^m,\quad
    \bm{f} = \begin{bmatrix} f_1\\ f_2\\ \vdots\\ f_m \end{bmatrix} \in \mathbb{R}^m,\quad
    \bm{y}_0 = \bm{y}(t)\big|_{t=a} \in \mathbb{R}^m
\end{equation*}
分别为待求的状态变量列阵（解向量），已知的状态变化率函数（向量函数）和已知的状态变量列阵初值（初始向量）。


\parahead{一阶常微分方程组初值问题数值解法（一）——线性多步法}

线性多步法 (Linear multistep method)：
\begin{equation}
    \bm{y}_n = \sum_{i=1}^{k}\alpha_i\,\bm{y}_{n-i} + h\sum_{i=0}^{k}\beta_i\,\bm{f}(t_{n-i},\bm{y}_{n-i}),
    \label{eq:linear_multistep}
\end{equation}
当 $\beta_0 = 0$ 时为显式公式，当 $\beta_0 \neq 0$ 为隐式公式。当 $k = 1$ 时退化为单步法。


\parahead{一阶常微分方程组初值问题数值解法（二）——预估校正法}

预估校正法（predictor–corrector method）：
\begin{align}
    \bar{\bm{y}}_n &= \sum_{i=1}^{k}\bar{\alpha}_i\,\bm{y}_{n-i} + h\sum_{i=1}^{k}\bar{\beta}_i\,\bm{f}(t_{n-i},\bm{y}_{n-i}), \tag{I-a}\label{eq:pc_predictor}\\[6pt]
    \bm{y}_n &= \sum_{i=1}^{k}\alpha_i\,\bm{y}_{n-i} + h\sum_{i=1}^{k}\beta_i\,\bm{f}(t_{n-i},\bm{y}_{n-i}) + h\beta_0\,\bm{f}(t_n,\bar{\bm{y}}_n). \tag{I-b}\label{eq:pc_corrector}
\end{align}
式~(\ref{eq:pc_predictor})为显式预估公式，式~(\ref{eq:pc_corrector})为隐式校正公式。


\parahead{一阶常微分方程组初值问题数值解法（三）——龙格-库塔法}

龙格-库塔法 (Runge-Kutta method)：
\begin{align}
    \bm{y}_n &= \bm{y}_{n-1} + h\sum_{i=1}^{s}b_i\,\bm{k}_i, \tag{II-a}\label{eq:rk_general}\\[6pt]
    \bm{k}_i &= \bm{f}\!\left(t_{n-1}+c_i\,h,\;\bm{y}_{n-1}+h\sum_{j=1}^{s}a_{i,j}\,\bm{k}_j\right). \tag{II-b}\label{eq:rk_ki}
\end{align}

\begin{remark}
如果当 $j \geq i$ 时 $a_{i,j} = 0$，式~(\ref{eq:rk_general})和~(\ref{eq:rk_ki})就是显式 RK 方法。如果当 $j > i$ 时 $a_{i,j} = 0$，而对角元素 $a_{i,i}$ 不全为 $0$，式~(\ref{eq:rk_general})和~(\ref{eq:rk_ki})就称为对角隐式 RK 方法；若此时 $a_{i,i}$ 均相等，则称为对角隐式 RK 方法。
\end{remark}

经典四阶定步长龙格-库塔法 (Classical fourth-order fixed-step Runge-Kutta method, RK4)：
\begin{align}
    \bm{y}_n &= \bm{y}_{n-1} + \frac{h}{6}(\bm{k}_1 + 2\bm{k}_2 + 2\bm{k}_3 + \bm{k}_4), \label{eq:rk4}\\
    \bm{k}_1 &= \bm{f}(t_{n-1},\;\bm{y}_{n-1}), \label{eq:rk4_k1}\\
    \bm{k}_2 &= \bm{f}\!\left(t_{n-1}+\frac{h}{2},\;\bm{y}_{n-1}+\frac{h}{2}\bm{k}_1\right), \label{eq:rk4_k2}\\
    \bm{k}_3 &= \bm{f}\!\left(t_{n-1}+\frac{h}{2},\;\bm{y}_{n-1}+\frac{h}{2}\bm{k}_2\right), \label{eq:rk4_k3}\\
    \bm{k}_4 &= \bm{f}\!\left(t_{n-1}+h,\;\bm{y}_{n-1}+h\bm{k}_3\right). \label{eq:rk4_k4}
\end{align}


\parahead{微分代数方程初值问题数值求解}

已知多体系统动力学微分代数方程组
\begin{equation*}
    \begin{cases}
        \bm{M}\ddot{\bm{q}} + \bm{c}_q^{\mathrm{T}}\bm{\lambda} = \bm{h},\\[4pt]
        \bm{c}(\bm{q},t) = \bm{0},
    \end{cases}
\end{equation*}
及其初始条件（定解条件）
\begin{equation*}
    \begin{cases}
        \bm{q}(t)\big|_{t=t_0} = \bm{q}_0,\\[4pt]
        \dot{\bm{q}}(t)\big|_{t=t_0} = \dot{\bm{q}}_0,
    \end{cases}
\end{equation*}
如何对其动力学响应进行数值求解。


\parahead{位置级、速度级与加速度级系统约束方程}

位置级约束方程
\begin{equation}
    \bm{0} = \bm{c}(\bm{q},t).
    \label{eq:constraint_position}
\end{equation}
对时间求一阶导数可得速度级约束方程
\begin{equation}
    \bm{0} = \bm{c}_q\dot{\bm{q}} + \bm{c}_t.
    \label{eq:constraint_velocity}
\end{equation}
速度级约束方程对时间求一阶导数可得加速度级约束方程，即
\begin{equation}
    \bm{0} = \bm{c}_q\ddot{\bm{q}} + \dot{\bm{c}}_q\dot{\bm{q}} + \dot{\bm{c}}_t =: \bm{c}_q\ddot{\bm{q}} - \bm{\varepsilon},
    \label{eq:constraint_acceleration}
\end{equation}
式中
\begin{equation*}
    \bm{\varepsilon}(\bm{q},\dot{\bm{q}},t) \triangleq -\dot{\bm{c}}_q\dot{\bm{q}} - \dot{\bm{c}}_t.
\end{equation*}


\parahead{系统动力学方程}

多体系统运动微分方程：
\begin{equation*}
    \bm{M}\ddot{\bm{q}} + \bm{c}_q^{\mathrm{T}}\bm{\lambda} = \bm{h},
\end{equation*}
多体系统约束方程：
\begin{equation*}
    \begin{cases}
        \bm{0} = \bm{c},\\[3pt]
        \bm{0} = \bm{c}_q\dot{\bm{q}} + \bm{c}_t,\\[3pt]
        \bm{0} = \bm{c}_q\ddot{\bm{q}} - \bm{\varepsilon},
    \end{cases}
\end{equation*}
式中
\begin{align*}
    &\bm{q}(t) \in \mathbb{R}^n,\;\dot{\bm{q}}(t) \in \mathbb{R}^n,\;\bm{M}(\bm{q},t) \in \mathbb{R}^{n\times n},\;\bm{h}(\bm{q},\dot{\bm{q}},t) \in \mathbb{R}^n,\\
    &\bm{c}(\bm{q},t) \in \mathbb{R}^m,\;\bm{c}_q(\bm{q},t) \in \mathbb{R}^{m\times n},\;\bm{c}_t(\bm{q},t) \in \mathbb{R}^m,\;\bm{\varepsilon}(\bm{q},\dot{\bm{q}},t) \in \mathbb{R}^m,\\
    &\ddot{\bm{q}}(t) \in \mathbb{R}^n,\;\bm{\lambda} \in \mathbb{R}^m.
\end{align*}


\parahead{二阶常微分方程组形式的系统动力学方程}

联立系统运动微分方程
\begin{equation*}
    \bm{M}\ddot{\bm{q}} + \bm{c}_q^{\mathrm{T}}\bm{\lambda} = \bm{h}(\bm{q},\dot{\bm{q}},t)
\end{equation*}
与加速度级约束方程
\begin{equation*}
    \bm{0} = \bm{c}_q\ddot{\bm{q}} - \bm{\varepsilon},
\end{equation*}
可得
\begin{equation}
    \begin{bmatrix}
        \bm{M} & \bm{c}_q^{\mathrm{T}}\\[3pt]
        \bm{c}_q & \bm{O}
    \end{bmatrix}
    \begin{bmatrix}
        \ddot{\bm{q}}\\[3pt]
        \bm{\lambda}
    \end{bmatrix}
    =
    \begin{bmatrix}
        \bm{h}\\[3pt]
        \bm{\varepsilon}
    \end{bmatrix}.
    \label{eq:saddle_point_system}
\end{equation}
求解上述关于 $\ddot{\bm{q}}$ 和 $\bm{\lambda}$ 的线性代数方程组可得
\begin{equation}
    \ddot{\bm{q}} = \bm{f}_{\text{acc}}(\bm{q},\dot{\bm{q}},t).
    \label{eq:acceleration_form}
\end{equation}


\parahead{一阶常微分方程组形式的系统动力学方程}

多体系统动力学二阶常微分方程组
\begin{equation*}
    \ddot{\bm{q}} = \bm{f}_{\text{acc}}(\bm{q},\dot{\bm{q}},t),
\end{equation*}
可转化为如下一阶微分方程组的形式
\begin{equation}
    \begin{bmatrix}
        \dot{\bm{y}}_1\\[3pt]
        \dot{\bm{y}}_2
    \end{bmatrix}
    =
    \begin{bmatrix}
        \bm{y}_2\\[3pt]
        \bm{f}_{\text{acc}}(\bm{y}_1,\bm{y}_2,t)
    \end{bmatrix},
    \qquad
    \begin{cases}
        \bm{y}_1 \triangleq \bm{q},\\[3pt]
        \bm{y}_2 \triangleq \dot{\bm{q}}.
    \end{cases}
    \label{eq:first_order_system}
\end{equation}

\begin{remark}
上述一阶常微分方程组的初值问题可借助于成熟的微分方程数值计算方法进行求解。计算机浮点数舍入误差以及微分方程数值求解中的截断误差会随着时间的推进逐步累积；由于该一阶常微分方程组未显含位置级和速度级的约束方程，位置级和速度级约束方程的违约量会逐步超过容许值。
\end{remark}


\parahead{违约与修正}

将微分方程数值解法求得的 $t_k$ 时刻的系统广义坐标记为 $\bm{q}_k$。若 $t_k$ 时刻位置级约束方程的违约量 $\bm{c}(\bm{q}_k,t_k)$ 超过了容许值时，则需要进行违约修正。

违约修正的思路为：在 $t_k$ 时刻，寻求一个最小的广义坐标增量 $\Delta\bm{q}_k$，使得修正后的广义坐标满足位置级约束方程，即
\begin{equation}
    \bm{0} = \bm{c}(\bm{q}_k + \Delta\bm{q}_k,t_k).
    \label{eq:constraint_correction}
\end{equation}
此处的"最小"可用二范数或无穷范数来度量。上述关于 $\Delta\bm{q}_k$ 的非线性代数方程组可通过牛顿-拉普森迭代方法进行求解。

由于速度级约束方程表现为广义速度的线性代数方程，因此，当速度级违约量超过容许值时，无需迭代过程即可单步完成广义速度修正。
