# 随机森林算法

> **理论基础** → 参见 [[../../机器学习数学基础/决策树与集成学习|决策树与集成学习]]

随机森林通过集成多个决策树投票决策，具有抗过拟合能力强、能处理高维数据的特点。

## 8. Python 实现
### 8.1 基本随机森林

```python
import numpy as np
import matplotlib.pyplot as plt
from sklearn.ensemble import RandomForestClassifier, RandomForestRegressor
from sklearn.datasets import make_classification
from sklearn.model_selection import train_test_split, cross_val_score
from sklearn.metrics import accuracy_score, classification_report

X, y = make_classification(n_samples=1000, n_features=20, n_informative=10,
                           n_redundant=5, n_clusters_per_class=2, random_state=42)
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=42)

rf = RandomForestClassifier(
    n_estimators=100,
    max_depth=None,
    min_samples_split=2,
    max_features='sqrt',
    oob_score=True,
    random_state=42,
    n_jobs=-1
)
rf.fit(X_train, y_train)

y_pred = rf.predict(X_test)
print(f"OOB Score: {rf.oob_score_:.4f}")
print(f"测试集准确率: {accuracy_score(y_test, y_pred):.4f}")
print(classification_report(y_test, y_pred))
```

### 8.2 树数量与性能

```python
n_trees_range = [1, 5, 10, 20, 50, 100, 200, 300, 500]
oob_scores = []
test_scores = []

for n_trees in n_trees_range:
    rf = RandomForestClassifier(n_estimators=n_trees, oob_score=True, random_state=42, n_jobs=-1)
    rf.fit(X_train, y_train)
    oob_scores.append(rf.oob_score_)
    test_scores.append(accuracy_score(y_test, rf.predict(X_test)))

fig, ax = plt.subplots(figsize=(10, 6))
ax.plot(n_trees_range, oob_scores, 'b-o', label='OOB 准确率')
ax.plot(n_trees_range, test_scores, 'r-o', label='测试集准确率')
ax.set_xlabel('树的数量')
ax.set_ylabel('准确率')
ax.set_title('随机森林：树的数量 vs 准确率')
ax.legend()
ax.grid(True, alpha=0.3)
plt.tight_layout()
plt.show()
```

### 8.3 特征重要性可视化

```python
rf_full = RandomForestClassifier(n_estimators=200, random_state=42, n_jobs=-1)
rf_full.fit(X_train, y_train)

importances_mdi = rf_full.feature_importances_
indices = np.argsort(importances_mdi)[::-1]

fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(14, 6))

ax1.barh(range(len(importances_mdi)), importances_mdi[indices[::-1]], color='steelblue')
ax1.set_yticks(range(len(importances_mdi)))
ax1.set_yticklabels([f'Feature {i}' for i in indices[::-1]])
ax1.set_xlabel('MDI 重要性 (基于不纯度)')
ax1.set_title('特征重要性 (MDI)')

from sklearn.inspection import permutation_importance
perm_importance = permutation_importance(rf_full, X_test, y_test, n_repeats=10, random_state=42, n_jobs=-1)
perm_indices = np.argsort(perm_importance.importances_mean)[::-1]

ax2.boxplot(perm_importance.importances[perm_indices].T, vert=False,
            labels=[f'Feature {i}' for i in perm_indices])
ax2.set_xlabel('MDA 重要性 (基于排列)')
ax2.set_title('特征重要性 (排列)')

plt.tight_layout()
plt.show()

print("Top 10 特征 (MDI):")
for i in indices[:10]:
    print(f"  Feature {i}: {importances_mdi[i]:.4f}")
```

### 8.4 超参数调优

```python
from sklearn.model_selection import GridSearchCV

param_grid = {
    'n_estimators': [100, 200, 300],
    'max_depth': [None, 10, 20, 30],
    'min_samples_split': [2, 5, 10],
    'min_samples_leaf': [1, 2, 4],
    'max_features': ['sqrt', 'log2']
}

grid_search = GridSearchCV(
    RandomForestClassifier(random_state=42, n_jobs=-1),
    param_grid, cv=5, scoring='accuracy', n_jobs=-1, verbose=1
)
grid_search.fit(X_train, y_train)

print(f"最佳参数: {grid_search.best_params_}")
print(f"最佳CV准确率: {grid_search.best_score_:.4f}")
print(f"测试集准确率: {accuracy_score(y_test, grid_search.best_estimator_.predict(X_test)):.4f}")
```

### 8.5 OOB vs 交叉验证

```python
from sklearn.model_selection import cross_val_score

rf_oob = RandomForestClassifier(n_estimators=200, oob_score=True, random_state=42)
rf_oob.fit(X_train, y_train)
oob_acc = rf_oob.oob_score_

cv_scores = cross_val_score(
    RandomForestClassifier(n_estimators=200, random_state=42), X_train, y_train, cv=5
)

print(f"OOB 准确率: {oob_acc:.4f}")
print(f"5折CV 准确率: {cv_scores.mean():.4f} ± {cv_scores.std():.4f}")
```

### 8.6 决策边界可视化

```python
from sklearn.datasets import make_moons

X_moons, y_moons = make_moons(n_samples=500, noise=0.3, random_state=42)

fig, axes = plt.subplots(1, 3, figsize=(18, 5))

for ax, n_est, max_f in zip(axes, [1, 10, 100], [1, 2, 2]):
    rf_v = RandomForestClassifier(n_estimators=n_est, max_features=max_f, random_state=42)
    rf_v.fit(X_moons, y_moons)

    xx, yy = np.meshgrid(
        np.linspace(X_moons[:, 0].min() - 0.5, X_moons[:, 0].max() + 0.5, 300),
        np.linspace(X_moons[:, 1].min() - 0.5, X_moons[:, 1].max() + 0.5, 300)
    )
    Z = rf_v.predict(np.c_[xx.ravel(), yy.ravel()]).reshape(xx.shape)
    ax.contourf(xx, yy, Z, alpha=0.4, cmap='RdBu')
    ax.scatter(X_moons[y_moons == 0, 0], X_moons[y_moons == 0, 1], c='blue', s=15, alpha=0.6)
    ax.scatter(X_moons[y_moons == 1, 0], X_moons[y_moons == 1, 1], c='red', s=15, alpha=0.6)
    acc = accuracy_score(y_moons, rf_v.predict(X_moons))
    ax.set_title(f'{n_est}棵树 (准确率: {acc:.3f})')

plt.suptitle('随机森林决策边界（随树数量变化）', fontsize=14)
plt.tight_layout()
plt.show()
```

---

## 9. 随机森林回归
```python
from sklearn.metrics import mean_squared_error, r2_score

np.random.seed(42)
X_reg = np.sort(5 * np.random.rand(200, 1), axis=0)
y_reg = np.sin(X_reg).ravel() + np.random.normal(0, 0.2, X_reg.shape[0])

X_tr_r, X_te_r, y_tr_r, y_te_r = train_test_split(X_reg, y_reg, test_size=0.3, random_state=42)

rf_reg = RandomForestRegressor(n_estimators=100, random_state=42)
rf_reg.fit(X_tr_r, y_tr_r)

X_plot = np.linspace(X_reg.min(), X_reg.max(), 500).reshape(-1, 1)
y_plot = rf_reg.predict(X_plot)

y_pred_r = rf_reg.predict(X_te_r)

fig, ax = plt.subplots(figsize=(10, 6))
ax.scatter(X_tr_r, y_tr_r, s=15, alpha=0.5, label='训练')
ax.scatter(X_te_r, y_te_r, s=15, alpha=0.5, label='测试')
ax.plot(X_plot, y_plot, 'r-', linewidth=2, label='RF 预测')
ax.fill_between(X_plot.ravel(),
                rf_reg.predict(X_plot) - np.std([t.predict(X_plot) for t in rf_reg.estimators_], axis=0),
                rf_reg.predict(X_plot) + np.std([t.predict(X_plot) for t in rf_reg.estimators_], axis=0),
                alpha=0.2, color='red')
ax.set_title(f'随机森林回归 (R²={r2_score(y_te_r, y_pred_r):.3f})')
ax.legend()
plt.tight_layout()
plt.show()
```

---

## 10. 实际案例：特征选择
```python
from sklearn.feature_selection import SelectFromModel

np.random.seed(42)
n_samples, n_features = 500, 50
X_fs = np.random.randn(n_samples, n_features)
true_coefs = np.zeros(n_features)
true_coefs[:5] = [3, -2, 1.5, -1, 0.5]
y_fs = X_fs @ true_coefs + np.random.randn(n_samples) * 0.5

X_tr_fs, X_te_fs, y_tr_fs, y_te_fs = train_test_split(X_fs, y_fs, test_size=0.3, random_state=42)

rf_fs = RandomForestRegressor(n_estimators=200, random_state=42)
rf_fs.fit(X_tr_fs, y_tr_fs)

selector = SelectFromModel(rf_fs, threshold='median', prefit=True)
X_tr_selected = selector.transform(X_tr_fs)
X_te_selected = selector.transform(X_te_fs)

selected_features = np.where(selector.get_support())[0]
print(f"选择的特征: {selected_features}")
print(f"选择前: {X_tr_fs.shape[1]} 特征 → 选择后: {X_tr_selected.shape[1]} 特征")

from sklearn.linear_model import LinearRegression
lr_full = LinearRegression().fit(X_tr_fs, y_tr_fs)
lr_selected = LinearRegression().fit(X_tr_selected, y_tr_fs)
print(f"全部特征 R²: {lr_full.score(X_te_fs, y_te_fs):.4f}")
print(f"选择后 R²: {lr_selected.score(X_te_selected, y_te_fs):.4f}")
```

---

## 11. 优缺点总结
### 优点

1. **泛化能力强**：集成多棵树，不易过拟合
2. **对高维数据友好**：随机特征选择降低维度影响
3. **可处理缺失值**：通过代理分裂处理
4. **特征重要性**：自然提供特征重要性度量
5. **并行化**：各棵树独立训练，易于并行
6. **对异常值鲁棒**：Bootstrap 采样降低异常值影响
7. **无需特征缩放**：基于树的方法不依赖距离度量

### 缺点

1. **可解释性降低**：比单棵决策树更难解释
2. **存储开销大**：需要存储 $T$ 棵树
3. **预测速度**：需要遍历所有树
4. **对噪声大的回归问题**：可能过拟合
5. **对类别不平衡**：可能偏向多数类

---

## 12. 关键要点总结
| 要点 | 说明 |
|------|------|
| 核心思想 | Bagging + 随机特征选择 |
| 两个随机性 | Bootstrap 数据采样 + 随机特征子集 |
| 方差降低 | $\text{Var} = \rho\sigma^2 + \frac{(1-\rho)\sigma^2}{T}$ |
| OOB 估计 | 约 36.8% 未用数据可做验证，近似等价交叉验证 |
| 特征重要性 | MDI（基于不纯度）和 MDA（基于排列） |
| 默认参数 | $m = \sqrt{d}$（分类），$m = d/3$（回归） |
| 树数量 | 越多越好（但收益递减），通常 100-500 |
| 过拟合控制 | 不剪枝，靠集成降低过拟合 |
| 适用场景 | 中等规模数据，需要特征重要性，基线模型 |

随机森林是机器学习中最常用的算法之一，以其**稳定性好、效果可靠、调参简单**而著称，是实际项目中首选的基线模型之一。

## 跨领域关联
- **人工智能**：[[../../../../人工智能/机器学习与深度学习/1.机器学习概述|机器学习（集成学习核心算法）]] | [[../../../../人工智能/深度学习教程/00-深度学习索引|深度学习（Bagging vs 深度学习）]]
- **计算机类**：[[../../../../计算机类/python学习/01-Python基础与数据类型|Python sklearn.ensemble 实现]]
