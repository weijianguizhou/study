# 动态规划

> **理论基础** → 参见 [[../../优化方法/动态规划|动态规划理论]]

动态规划将多阶段决策问题分解为子问题，通过Bellman方程逐阶段求解，广泛用于路径规划、资源分配。

## 四、经典动态规划问题
### 4.1 背包问题

**0-1 背包问题**：有 $n$ 个物品，第 $i$ 个物品重量为 $w_i$，价值为 $v_i$，背包容量为 $W$。选择若干物品装入背包，使总价值最大。

**状态定义**：$V(i, j)$ 为考虑前 $i$ 个物品、背包剩余容量为 $j$ 时的最大价值。

**Bellman 方程**：

$$
V(i, j) = \begin{cases}
0, & i = 0 \text{ 或 } j = 0 \\
V(i-1, j), & w_i > j \\
\max\{V(i-1, j), V(i-1, j-w_i) + v_i\}, & w_i \leq j
\end{cases}
$$

**最优值**：$V(n, W)$

**时间复杂度**：$O(nW)$

**空间复杂度**：$O(nW)$（可用滚动数组优化到 $O(W)$）

```python
def knapsack_01(weights, values, W):
    n = len(weights)
    dp = [0] * (W + 1)
    
    for i in range(n):
        for j in range(W, weights[i] - 1, -1):  # 逆序遍历
            dp[j] = max(dp[j], dp[j - weights[i]] + values[i])
    
    return dp[W]
```

### 4.2 最短路径问题

给定有向图 $G = (V, E)$，求从源点 $s$ 到终点 $t$ 的最短路径。

**Bellman 方程**：

$$
V(v) = \min_{(v, u) \in E} \{w(v, u) + V(u)\}
$$

其中 $V(u)$ 为从节点 $u$ 到 $t$ 的最短距离，$w(v, u)$ 为边 $(v, u)$ 的权重。

**Bellman-Ford 算法**（可处理负权边）：

$$
d[v] = \min(d[v], d[u] + w(u, v)), \quad \forall (u, v) \in E
$$

重复 $|V|-1$ 次松弛操作。

### 4.3 矩阵链乘法

给定 $n$ 个矩阵 $A_1, A_2, \ldots, A_n$，其中 $A_i$ 的维度为 $p_{i-1} \times p_i$，确定矩阵乘法的最优括号化方案使标量乘法次数最少。

**状态定义**：$m[i, j]$ 为计算 $A_i A_{i+1} \cdots A_j$ 的最少乘法次数。

**Bellman 方程**：

$$
m[i, j] = \begin{cases}
0, & i = j \\
\min_{i \leq k < j} \{m[i, k] + m[k+1, j] + p_{i-1} p_k p_j\}, & i < j
\end{cases}
$$

**时间复杂度**：$O(n^3)$

**空间复杂度**：$O(n^2)$

### 4.4 最长公共子序列（LCS）

给定两个序列 $X = x_1 x_2 \cdots x_m$ 和 $Y = y_1 y_2 \cdots y_n$，求它们的最长公共子序列。

**Bellman 方程**：

$$
c[i, j] = \begin{cases}
0, & i = 0 \text{ 或 } j = 0 \\
c[i-1, j-1] + 1, & x_i = y_j \\
\max\{c[i-1, j], c[i, j-1]\}, & x_i \neq y_j
\end{cases}
$$

### 4.5 编辑距离

将字符串 $A$ 转换为字符串 $B$ 所需的最少操作数（插入、删除、替换）。

**Bellman 方程**：

$$
d[i, j] = \begin{cases}
j, & i = 0 \\
i, & j = 0 \\
d[i-1, j-1], & a_i = b_j \\
1 + \min\{d[i-1, j], d[i, j-1], d[i-1, j-1]\}, & a_i \neq b_j
\end{cases}
$$

---

## 八、Python 代码实现
### 8.1 0-1 背包问题

```python
def knapsack_01_dp(weights, values, W):
    """0-1背包问题 - 完整DP实现"""
    n = len(weights)
    dp = [[0] * (W + 1) for _ in range(n + 1)]
    
    # 填表
    for i in range(1, n + 1):
        for j in range(W + 1):
            dp[i][j] = dp[i-1][j]  # 不选第i个物品
            if j >= weights[i-1]:
                dp[i][j] = max(dp[i][j], dp[i-1][j-weights[i-1]] + values[i-1])
    
    # 回溯最优解
    items = []
    j = W
    for i in range(n, 0, -1):
        if dp[i][j] != dp[i-1][j]:
            items.append(i - 1)
            j -= weights[i - 1]
    
    items.reverse()
    return dp[n][W], items

# 示例
weights = [2, 3, 4, 5]
values = [3, 4, 5, 6]
W = 8
max_val, items = knapsack_01_dp(weights, values, W)
print(f"最大价值: {max_val}")
print(f"选择的物品: {items}")
```

### 8.2 最短路径（Bellman-Ford）

```python
def bellman_ford(V, edges, source, target):
    """Bellman-Ford 算法"""
    dist = {v: float('inf') for v in V}
    prev = {v: None for v in V}
    dist[source] = 0
    
    # 松弛 |V|-1 次
    for _ in range(len(V) - 1):
        for u, v, w in edges:
            if dist[u] + w < dist[v]:
                dist[v] = dist[u] + w
                prev[v] = u
    
    # 回溯路径
    path = []
    current = target
    while current is not None:
        path.append(current)
        current = prev[current]
    path.reverse()
    
    return dist[target], path

# 示例
V = ['A', 'B', 'C', 'D', 'E']
edges = [
    ('A', 'B', 4), ('A', 'C', 2),
    ('B', 'C', 1), ('B', 'D', 5),
    ('C', 'D', 8), ('C', 'E', 10),
    ('D', 'E', 2), ('D', 'A', -3)  # 负权边
]

dist, path = bellman_ford(V, edges, 'A', 'E')
print(f"最短距离: {dist}")
print(f"路径: {' -> '.join(path)}")
```

### 8.3 矩阵链乘法

```python
def matrix_chain_order(p):
    """矩阵链乘法最优括号化"""
    n = len(p) - 1
    m = [[0] * n for _ in range(n)]
    s = [[0] * n for _ in range(n)]
    
    for length in range(2, n + 1):  # 链长度
        for i in range(n - length + 1):
            j = i + length - 1
            m[i][j] = float('inf')
            for k in range(i, j):
                q = m[i][k] + m[k+1][j] + p[i] * p[k+1] * p[j+1]
                if q < m[i][j]:
                    m[i][j] = q
                    s[i][j] = k
    
    def print_optimal_parens(s, i, j):
        if i == j:
            return f"A{i+1}"
        k = s[i][j]
        left = print_optimal_parens(s, i, k)
        right = print_optimal_parens(s, k+1, j)
        return f"({left} x {right})"
    
    return m[0][n-1], print_optimal_parens(s, 0, n-1)

# 示例: A1(30x35) x A2(35x15) x A3(15x5) x A4(5x10) x A5(10x20) x A6(20x25)
p = [30, 35, 15, 5, 10, 20, 25]
cost, formula = matrix_chain_order(p)
print(f"最少乘法次数: {cost}")
print(f"最优括号化: {formula}")
```

### 8.4 编辑距离

```python
def edit_distance(s1, s2):
    """编辑距离"""
    m, n = len(s1), len(s2)
    dp = [[0] * (n + 1) for _ in range(m + 1)]
    
    for i in range(m + 1):
        dp[i][0] = i
    for j in range(n + 1):
        dp[0][j] = j
    
    for i in range(1, m + 1):
        for j in range(1, n + 1):
            if s1[i-1] == s2[j-1]:
                dp[i][j] = dp[i-1][j-1]
            else:
                dp[i][j] = 1 + min(
                    dp[i-1][j],      # 删除
                    dp[i][j-1],      # 插入
                    dp[i-1][j-1]     # 替换
                )
    
    return dp[m][n]

print(f"编辑距离: {edit_distance('kitten', 'sitting')}")  # 3
print(f"编辑距离: {edit_distance('saturday', 'sunday')}")  # 3
```

---

## 九、MATLAB 代码实现
```matlab
% 动态规划 - 背包问题
function [maxVal, items] = knapsack_dp(weights, values, W)
    n = length(weights);
    dp = zeros(n+1, W+1);
    
    for i = 1:n
        for j = 0:W
            dp(i+1, j+1) = dp(i, j+1);
            if j >= weights(i)
                dp(i+1, j+1) = max(dp(i+1, j+1), dp(i, j+1-weights(i)) + values(i));
            end
        end
    end
    
    maxVal = dp(n+1, W+1);
    
    % 回溯
    items = [];
    j = W;
    for i = n:-1:1
        if dp(i+1, j+1) ~= dp(i, j+1)
            items = [items, i];
            j = j - weights(i);
        end
    end
    items = fliplr(items);
end

% 测试
weights = [2, 3, 4, 5];
values = [3, 4, 5, 6];
W = 8;
[maxVal, items] = knapsack_dp(weights, values, W);
fprintf('最大价值: %d\n', maxVal);
fprintf('选择物品: ');
disp(items);
```

---

## 十、关键要点总结
1. **最优子结构**和**重叠子问题**是动态规划的两个核心特征
2. **Bellman 方程**是动态规划的理论核心，体现了"最优性原理"
3. 背包问题、最短路径、矩阵链乘法、LCS是经典DP应用
4. 动态规划的时间复杂度通常为 $O(n \cdot W)$ 或 $O(n^2)$ 到 $O(n^3)$
5. **滚动数组**可以将空间复杂度降低
6. 随机动态规划（MDP）广泛用于强化学习
7. 状态定义是设计DP算法的关键，好的状态定义能大幅简化问题
8. DP与贪心、分治的关系：DP通过存储子问题解避免重复计算，而贪心只做局部最优选择
