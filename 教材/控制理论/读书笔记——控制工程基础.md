*We don’t need to be perfect; we just need to be good enough to meet our specifications.*
# 控制工程基础
## 控制系统基础知识
### 增益(Gain)
增益是一个比例值，它显示了稳态时输入信号的大小与输出信号的大小之间的关系。许多系统包含一种可以改变增益的方法，为系统提供或多或少的“功率（*power*）”。

下图显示了一个具有假设输入和输出的系统。由于输出是输入幅度的两倍，因此系统的增益为2。
![[增益为K=2的系统的演示.png]]
### 方框图（Block diagrams）
方框图是设计和分析一个控制系统的有力的工具，可以被系统地操纵和简化。一般的形式为：
![[带命名法的框图.png]]
开环增益是从输入(圆)处的和节点到输出支路的总增益。如果反馈回路断开，这将是系统的增益。

反馈增益是从输出返回到输入和节点的总增益。求和节点的输出是其输入的总和。
#### 级联块（Cascaded blocks）
表示关系：$Y=(P_1P_2)X$
图示形式:![[级联块.png]]
![[简单级联块.png]]
#### 并行组合块（Combining blocks in parallel）
表示关系：$Y=P_1X \pm P_2X$
图示形式：
![[并行块.png]]
![[简单并行块.png]]

#### 从前馈循环中删除块
表示关系：$Y=P_1X \pm P_2X$
图示关系：
![[前馈环.png]]
![[变换的前馈环.png]]
#### 消除反馈回路
表示关系：$Y=P_1(X\mp P_2Y)$
图示关系：
![[反馈环.png]]
![[变换的反馈环.png|700]]
### 开环和闭环系统
> [!definition] Plant
> 放置被控制的执行器的系统或集合。

> [!definition] 控制器（Controller）
> 用来驱动 *Plant* 从现在的状态到希望的状态（基准状态），即通过将参考信号和输出之间的差值驱动到零来将输入施加到对象以实现所需的系统状态。

> [!definition] 基准（reference）
> 系统所需的状态。该值用作控制器误差计算的参考点，用符号 $r(t)$ 表示。

> [!definition] 控制输入（control input）
> 用于控制目的的设备的输入。用符号 $u(t)$ 表示。

> [!definition] 误差（error）
> 基准减去输出或状态，用符号 $e(t)$ 表示。

> [!definition] 输出（output）
> 传感器的测量结果。用符号 $y(t)$ 表示。

> [!definition] 开环控制器（前馈控制器）
> 不包含 *Plant* 的测量结果信息的控制器。
下图是一个含前馈控制器的*plant*
![[开环控制系统.png]]

> [!definition] 闭环系统（反馈控制器）
> 包含从对象输出反馈的信息的控制器。

![[闭环控制系统.png]]
注意，系统的输入和输出是从*plant*的角度定义的。图中所示的负反馈控制器将基准与输出之间的差值，即误差变为0

### 前馈
反馈控制可以有效地用于基准跟踪(使系统的输出跟随期望的参考信号)，但它是一种反应性措施；直到系统已经落后时，系统才会开始努力工作😭。

前馈控制器注入有关系统动态的信息或期望的运动。前馈控制器处理控制动作的一部分。前馈控制器注入有关系统动态或所需运动的信息。前馈处理我们已经知道必须应用的控制操作的一部分，以使系统跟踪引用，然后反馈补偿我们在运行时不知道或不能知道的系统行为。

有两种类型的前馈：
1. 基于模型的前馈
2. 用于未建模动态的前馈。
第一个是求解满足所需速度和加速度所需输入的系统的数学模型。
第二个直接补偿未建模的力或行为，因此反馈控制器没有必要进行操作。
这两种类型都可以使反馈控制器更简单。

# PID控制器
PID控制器是一种常用的反馈控制器，由比例项（*proportional*）、积分项（*integral*）和导数项（*derivative*）组成，因此得名。

下面说明一些术语：
- 基准(*reference*)称为**设计点**(*setpoint*)
- 输出(output)称为**过程变量**（*process variable*）或**测量位置**（*measured position*）
然后误差写作$e(t)=r(t)-y(t)$
下面将对关心的不同物理量应用比例控制器。这将对恒定和移动设定值的导数项的行为提供更完整的解释，这种直觉将延续到本书后面介绍的现代控制方法。
>看完书能回答这个问题吗？

比例项将位置误差驱动为零，导数项将速度误差驱动为零，积分项累积速度误差为零，积分项累积设定点和输出曲线之间的面积(位置误差的积分)，并将当前总和添加到控制输入。下面将对其中的每一个进行更详细的介绍。

## 比例项（*Proportional term*）
比例项使位置误差变为零。

![[比例控制器 1.png]]

> [!definition] 比例控制器
> $$u(t)=K_{p}e(t)$$
> 这里：
> - $K_{p}$为比例增益
> - $e(t)$是当前时间$t$的误差

![[比例控制器 1.png]]
比例增益就像一个可以被软件定义的弹簧(Software-defined springs)，把系统向目标的位置拽。

对比一下来理解一下：
力学里弹簧弹力满足：$F=k(r-x)$
这里的控制量$u(t)$满足$u(t)=K_{p}e(t)=K_{p}(r(t)-y(t))$
这样看来，比例控制器就像一根弹簧，初始位置在目标位置，不断地把系统的位置向目标位置拽，直到系统位置误差为0.
>想起来大二下学期一个寻常的周四下午，和张老师和刘方鑫师兄看师兄的毕业论文的时候，张老师画的图，原来是这样。恍如隔世。

## 导数项（*Derivative term*）
导数项使速度误差变为零。
> [!definition] PD控制器
> $$u(t)=K_{p}e(t)+K_{d}\frac{de}{dt}$$
> 这里：
> - $K_{p}$是比例增益
> - $K_{d}$是导数增益

![[PD控制器.png]]

