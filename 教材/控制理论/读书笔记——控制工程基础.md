*We don’t need to be perfect; we just need to be good enough to meet our specifications.*
# 控制工程基础
## 控制系统基础知识
### 增益(Gain)
增益是一个比例值，它显示了稳态时输入信号的大小与输出信号的大小之间的关系。许多系统包含一种可以改变增益的方法，为系统提供或多或少的“功率（*power*）”。

下图显示了一个具有假设输入和输出的系统。由于输出是输入幅度的两倍，因此系统的增益为2。
![[增益为K=2的系统的演示.png]]
### 方框图（Block diagrams）
方框图是设计和分析一个控制系统的有力的工具，可以被系统地操纵和简化。一般的形式为：
![[带命名法的框图.png]]
开环增益是从输入(圆)处的和节点到输出支路的总增益。如果反馈回路断开，这将是系统的增益。

反馈增益是从输出返回到输入和节点的总增益。求和节点的输出是其输入的总和。
#### 级联块（Cascaded blocks）
表示关系：$Y=(P_1P_2)X$
图示形式:![[级联块.png]]
![[简单级联块.png]]
#### 并行组合块（Combining blocks in parallel）
表示关系：$Y=P_1X \pm P_2X$
图示形式：
![[并行块.png]]
![[简单并行块.png]]

#### 从前馈循环中删除块
表示关系：$Y=P_1X \pm P_2X$
图示关系：
![[前馈环.png]]
![[变换的前馈环.png]]
#### 消除反馈回路
表示关系：$Y=P_1(X\mp P_2Y)$
图示关系：
![[反馈环.png]]
![[变换的反馈环.png|700]]
### 开环和闭环系统
放置被控制的执行器的系统或集合称为*Plant*。

**控制器**（*Controller*） 是用来驱动*Plant* 从现在的状态到希望的状态（基准状态），即通过将参考信号和输出之间的差值驱动到零来将输入施加到对象以实现所需的系统状态。

**基准**（*reference*）系统所需的状态。该值用作控制器误差计算的参考点，用符号$r(t)$表示。

**控制输入**（*control input*）用于控制目的的设备的输入。用符号$u(t)$表示

**误差**（*error*） 基准减去输出或状态,用符号$e(t)$表示。

**输出**（*output*）传感器的测量结果。用符号$y(t)$表示。

不包含*Plants*的测量结果信息的控制器称为**开环控制器**或**前馈控制器**。
下图是一个含前馈控制器的*plant*
![[开环控制系统.png]]

包含从对象输出反馈的信息的控制器称为**闭环系统**或**反馈控制器**。![[闭环控制系统.png]]
注意，系统的输入和输出是从*plant*的角度定义的。图中所示的负反馈控制器将基准与输出之间的差值，即误差变为0

### 前馈
反馈控制可以有效地用于基准跟踪(使系统的输出跟随期望的参考信号)，但它是一种反应性措施；直到系统已经落后时，系统才会开始努力工作😭。

前馈控制器注入有关系统动态的信息或期望的运动。前馈控制器处理控制动作的一部分。前馈控制器注入有关系统动态或所需运动的信息。前馈处理我们已经知道必须应用的控制操作的一部分，以使系统跟踪引用，然后反馈补偿我们在运行时不知道或不能知道的系统行为。

有两种类型的前馈：
1. 基于模型的前馈
2. 用于未建模动态的前馈。
第一个是求解满足所需速度和加速度所需输入的系统的数学模型。
第二个直接补偿未建模的力或行为，因此反馈控制器没有必要进行操作。
这两种类型都可以使反馈控制器更简单。

# PID控制器
PID控制器是一种常用的反馈控制器，由比例项（*proportional*）、积分项（*integral*）和导数项（*derivative*）组成，因此得名。