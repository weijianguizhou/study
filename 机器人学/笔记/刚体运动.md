我们知道要描述一个刚体在三维空间中的位置与姿态至少需要6个参数。

根据笔记[[09-螺旋运动]]中的证明，我们知道*Chasles-Mozzi*定理：任意刚体运动都可以通过绕某一螺旋轴旋转一定的角度和平移来实现。
## 刚体运动与运动旋量
### 齐次变换矩阵(*Homogeneous Transformation Matrices*)
接下来，我们探讨刚体姿态与位置联合表示的构建方法。一种自然的选择是使用旋转矩阵 $\boldsymbol{A}\in SO(3)$   来表示体坐标系 $\{b\}$ 在固定坐标系$\{s\}$中的朝向，以及使用向量 $\boldsymbol{p} \in \mathbb{R}^3$ 来表示 $\{b\}$ 在 $\{s\}$ 中的原点。而非将 $\boldsymbol{A}$ 与 $\boldsymbol{p}$ 分别表示，我们可以将它们封装成一个单一矩阵.

特殊欧几里得群 $SE(3)$（亦称刚体运动群或 $\mathbb{R}^3$ 中的齐次变换矩阵群）是指所有形如以下形式的 $4 \times 4$ 实矩阵 $\boldsymbol{T}$ 的集合：
$$\boldsymbol{T}=\begin{bmatrix}
  \boldsymbol{A}& \boldsymbol{p}\\
  0&1
\end{bmatrix}=\begin{bmatrix}
  a_{11}&a_{12}  &a_{13}  &p_1 \\
  a_{21}&a_{22}  &a_{23}  &p_2 \\
  a_{31}&a_{32}  &a_{33}  &p_3 \\
  0&0  &0  &1
\end{bmatrix}$$
有的时候齐次变换矩阵$\boldsymbol{T}$也会用这样一种表达方式$(\boldsymbol{T},\boldsymbol{p})$

### 齐次变换矩阵的性质
#### 性质一
齐次变换矩阵的逆也是齐次变换矩阵。
$$\boldsymbol{T}=\begin{bmatrix}
  \boldsymbol{A}& \boldsymbol{p}\\
  0&1
\end{bmatrix}^{-1}=\begin{bmatrix}
  \boldsymbol{A}^{\mathrm{T} } & -\boldsymbol{A}^{\mathrm{T} }\boldsymbol{p}\\
  0&1
\end{bmatrix}$$
#### 性质二
两个齐次变换矩阵的乘积也是齐次变换矩阵。
#### 性质三
齐次变换矩阵的乘法满足结合律，但一般不满足交换律。

#### 性质四
**齐次坐标**：称
$$\begin{bmatrix}
  \boldsymbol{x}\\1
\end{bmatrix}$$
为齐次坐标。

则有：
$$\boldsymbol{T}\begin{bmatrix}
  \boldsymbol{x}\\1
\end{bmatrix}=\begin{bmatrix}
  \boldsymbol{A}& \boldsymbol{p}\\
  0&1
\end{bmatrix}\begin{bmatrix}
  \boldsymbol{x}\\1
\end{bmatrix}=\begin{bmatrix}
\boldsymbol{A}\boldsymbol{x}+\boldsymbol{p}\\1
\end{bmatrix}$$
在不引起歧义的情况下$\boldsymbol{T}\boldsymbol{x}=\boldsymbol{A}\boldsymbol{x}+\boldsymbol{p}$
给定 $T=(R,p)\in \mathrm{SE}(3)$ 以及 $x,y\in\mathbb{R}^{3}$，下列结论成立：
(a) $\|Tx-Ty\|=\|x-y\|$，其中 $\|\cdot\|$ 表示 $\mathbb{R}^{3}$ 中的标准欧几里得范数，即 $\|x\|=\sqrt{x^{\mathrm{T}}x}$。
(b) 对所有 $z\in\mathbb{R}^{3}$，有 $\langle Tx-Tz,Ty-Tz\rangle=\langle x-z,y-z\rangle$，其中 $\langle\cdot,\cdot\rangle$ 表示 $\mathbb{R}^{3}$ 中的标准欧几里得内积，$\langle x,y\rangle=x^{\mathrm{T}}y$。


由上述描述其实可以看出，齐次变换矩阵$\boldsymbol{T}$能将一个点$\boldsymbol{x}$变换到$\boldsymbol{T}\boldsymbol{x}$

结论（a）表明这个变换是等距的，而性质(b)表明这个变换是保角的。

### 齐次变换矩阵的用途
#### 表示一个刚体的位形
#### 变换参考坐标系
#### 表示一个向量或者坐标系的位移
