# 布尔代数
研究数字电路的基础为逻辑代数，由英国数学家*George Boole*在1847年提出的，逻辑代数也称布尔代数。

## 基本逻辑运算
在逻辑代数中,变量常用字母$A,B,C,……Y,Z, a,b,c,……x,y,z$等表示，变量的取值只能是“0”或“1”

逻辑代数中只有三种基本逻辑运算,即“与” 、“或” 、 “非”。

### 与
只有决定一事件的**全部条件**都具备时，这件事**才成立**；如果有一个或一个以上条件不具备，则这件事就不成立。这样的因果关系称为“与”逻辑关系。

与门逻辑符号如下。
![[与逻辑电路.png]]
与逻辑运算真值表如下：

### 或
在决定一事件的各种条件中,只要有一个或一个以上条件具备时，这件事就成立;**只有**所有的条件**都不具备**时,这件事才**不成立**.这样的因果关系称为“或”逻辑关系。

或门逻辑符号如下：
![[或逻辑电路.png]]

**注意**：与门和或门均可以有多个输入端
### 非![[同或逻辑电路.png]]
假定事件$F$成立与否同条件$A$的具备与否有关,若$A$具备,则$F$不成立;若A不具备,则$F$成立.$F$和$A$之间的这种因果关系称为“非”逻辑关系.

非门逻辑符号如下
![[非逻辑电路.png]]

## 复合逻辑运算
### 与非逻辑
将与逻辑和非逻辑组合而成
![[与非逻辑电路.png]]
真值表如下：

### 或非逻辑
将或逻辑和非逻辑组合而成
![[或非逻辑电路.png]]
真值表如下：

### 与或非逻辑
由与、或、非三种逻辑组合而成
与或非逻辑函数式：$\mathbf{F}=\overline{\mathbf{AB}+\mathbf{CD}}$
![[与或非门逻辑符号.png]]
真值表如下：

### 异或逻辑
异或逻辑的函数式为：$\mathbf{F}=\overline{\mathbf{A}} \mathbf{B}+\mathbf{A} \overline{\mathbf{B}}=\mathbf{A} \oplus \mathbf{B}$

口诀：相同得“0”，相异得“1”。
真值表如下：
![[异或逻辑电路.png]]
### 同或逻辑
同或逻辑式为:$\mathbf{F}=\overline{\mathbf{A}} \overline{\mathbf{B}}+\mathbf{A} \mathbf{B}=\mathbf{A} \odot \mathbf{B}$

同或和异或互为反函数,即:
$$\mathbf{A} \oplus \mathbf{B}=\overline{\mathbf{A} \odot \mathbf{B}}$$
![[同或逻辑电路.png]]

## 逻辑电平及正、负逻辑
门电路的输入、输出为二值信号,用“0”和“1”表示。这里的“0” 、 “1”一般用两个不同电平值来表示。

**正逻辑**：若用高电平$V_{H}$表示逻辑“1” ,用低电平$V_{L}$表示逻辑“0” ,则称为正逻辑约定,简称正逻辑;

**负逻辑**：若用高电平$V_{H}$表示逻辑“0” ,用低电平$V_{L}$表示逻辑“1” ,则称为负逻辑约定,简称负逻辑。

一般采取**正逻辑**表示。

**注意**：对一个特定的逻辑门,采用不同的逻辑表示时,其门的名称也就不同.

## 基本定律和规则
### 逻辑函数的相等
设有两个逻辑:
$$F_{1}=f_{1}(A_{1},A_{2},A_{3},\dots ,A_{n})$$
$$F_{2}=f_{2}(A_{1},A_{2},A_{3},\dots ,A_{n})$$
如果对于$A_{1},A_{2},A_{3},\dots ,A_{n}$ 的任何一组取值（共$2^n$组），$F_{1}$和$F_{2}$均相等，则称$F_{1}$和$F_{2}$相等.

因此,如两个函数的真值表相等,则这两个函数一定相等.

### 基本定律

| 定律      | 与形式                                            | 或形式                                       |
| ------- | ---------------------------------------------- | ----------------------------------------- |
| ① 0-1 律 | $A\cdot0=0$                                    | $A+1=1$                                   |
| ② 自等律   | $A\cdot1=A$                                    | $A+0=A$                                   |
| ③ 重叠律   | $A\cdot A=A$                                   | $A+A=A$                                   |
| ④ 互补律   | $A\cdot\overline{A}=0$                         | $A+\overline{A}=1$                        |
| ⑤ 交换律   | $A\cdot B=B\cdot A$                            | $A+B=B+A$                                 |
| ⑥ 结合律   | $A(BC)=(AB)C$                                  | $A+(B+C)=(A+B)+C$                         |
| ⑦ 分配律   | $A(B+C)=AB+AC$                                 | $A+BC=(A+B)(A+C)$                         |
| ⑧ 反演律   | $\overline{A+B}=\overline{A}\cdot\overline{B}$ | $\overline{AB}=\overline{A}+\overline{B}$ |
反演律也称德·摩根定理,是一个非常有用的定理.

### 逻辑代数的三条规则
#### 代入规则
任何一个含有变量$x$的等式,如果将所有出现$x$的位置,都用一个逻辑函数式$F$代替,则等式仍然成立

#### 反演规则
设$F$为任意逻辑表达式,若将$F$中所有运算符、常量及变量作如下变换
![[反函数定义.png]]

则所得新的逻辑式即为$F$的反函数，记为$\overline{F}$

**注意**：
1. 保持原式运算的优先次序
2. 原式中的不属于单变量上的非号不变；

#### 对偶规则
设$F$为任意逻辑表达式,若将F中所有运算符和常量作如下变换：
![[对偶式定义.png]]
则所得新的逻辑表达式即为$F$的对偶式，记为$F’$ .