# 2026-08-30

## 控制工程基础：整合 PID 章节
- 将 `教材/控制理论/读书笔记——控制工程基础.md` 的「PID控制器」一节整合进 `控制理论/控制工程基础/第二章.tex`（此前为空；第一章「控制系统基础知识」此前已整合好）。
- 用户要求：不要改变公式与表述。已逐句对应，中文措辞与公式原样保留。
- 源 markdown 的「PD控制器就是两个P控制器的证明」推导含字符编码损坏：第4步有不平衡的 `((`、第5步有全角 `（`、第6步有多余 `)`。为能编译仅修正括号配对（`((`→`\(`，全角`（`→`(`，删末尾多余`)`），数学内容未改。
- 编译：`latexmk -xelatex` 通过，17 页，Overfull=0，无 undefined refs。图 `比例控制器 1.png`（含空格）XeLaTeX 可正常载入。
- 遗留：第一章 line 161 的 😭 emoji 在 lmroman 字体缺字（仅 warning，非我引入，不影响编译）。
